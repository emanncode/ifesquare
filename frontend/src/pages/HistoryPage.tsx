/**
 * History: list of past closed days + day detail.
 * Wired in milestone 9.
 */
export default function HistoryPage() {
  return (
    <div className="flex min-h-svh items-center justify-center p-6">
      <p className="text-muted-foreground text-sm">History page — coming soon</p>
    </div>
  )
}
