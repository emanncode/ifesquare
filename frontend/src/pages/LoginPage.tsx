/**
 * Login page — email + password for the single shop owner.
 * Wired in milestone 6 once auth API is live.
 */
export default function LoginPage() {
  return (
    <div className="flex min-h-svh items-center justify-center p-6">
      <p className="text-muted-foreground text-sm">Login page — coming soon</p>
    </div>
  )
}
