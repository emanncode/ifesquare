/**
 * Editable daily ledger table (opening / receipts / closing → sales / amount).
 * Debounced PATCH to /api/ledger/today/:productId in milestone 7.
 */
export function LedgerTable() {
  return null
}
